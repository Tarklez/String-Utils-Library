# String Utils Library

Простая библиотека для работы со строками на Python с модульными тестами.

## 📁 Структура проекта
project/  
├── string_utils.py # Основная библиотека функций  
└── test_string_utils.py # Unit-тесты для библиотеки

## 🚀 Функциональность

Библиотека предоставляет следующие функции:

- `reverse_string(s)` - переворачивает строку
- `capitalize_string(s)` - делает первую букву заглавной  
- `strip_string(s)` - удаляет пробелы в начале и конце
- `count_chars(s, char)` - подсчитывает количество символов
- `is_palindrome(s)` - проверяет, является ли строка палиндромом
- `replace_char(s, old, new)` - заменяет символы в строке
- `to_lower_case(s)` - преобразует к нижнему регистру
- `to_upper_case(s)` - преобразует к верхнему регистру
- `split_string(s, delimiter)` - разделяет строку по разделителю

## 🧪 Запуск тестов

```bash
# Запуск всех тестов
python -m unittest test_string_utils.py

# Запуск с подробным выводом
python -m unittest test_string_utils.py -v

# Запуск конкретного теста
python -m unittest test_string_utils.TestStringUtils.test_is_palindrome
